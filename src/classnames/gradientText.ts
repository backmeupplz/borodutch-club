import gradient from 'classnames/gradient'
import { classnames } from 'classnames/tailwind'

const gradientText = classnames(gradient, 'bg-clip-text', 'text-transparent')

export default gradientText
